<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme -->
    <style name="Base.Theme.MyRecylerView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- colors, typography, etc -->
    </style>

    <!-- Actual theme -->
    <style name="Theme.MyRecylerView" parent="Base.Theme.MyRecylerView" />

    <!-- Splash screen theme -->
    <style name="Theme.MyApp.MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/tes</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="duration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.MyRecylerView</item>
    </style>
</resources>
